# Onyx Craft Docker-sandbox overlay. Layer on top of docker-compose.yml.
# install.sh --include-craft wires this in automatically.
#
# Trust boundary: api_server, background, and sandbox-proxy all bind-mount
# /var/run/docker.sock (root-equivalent on host). Only run on hosts you fully
# control.
services:
  api_server:
    environment:
      - SANDBOX_BACKEND=${SANDBOX_BACKEND:-docker}
      # Public Onyx URL — internal compose hostnames don't resolve from the
      # sandbox bridge.
      - SANDBOX_API_SERVER_URL=${SANDBOX_API_SERVER_URL:-}
      - SANDBOX_CONTAINER_IMAGE=${SANDBOX_CONTAINER_IMAGE:-onyxdotapp/sandbox:v0.1.50}
      - SANDBOX_DOCKER_NETWORK=${SANDBOX_DOCKER_NETWORK:-onyx_craft_sandbox}
      - SANDBOX_DOCKER_MEMORY_LIMIT=${SANDBOX_DOCKER_MEMORY_LIMIT:-2g}
      - SANDBOX_DOCKER_CPU_LIMIT=${SANDBOX_DOCKER_CPU_LIMIT:-1.0}
      - ENABLE_OPENCODE_DEBUGGING=${ENABLE_OPENCODE_DEBUGGING:-false}
      # Egress proxy wiring. Setting SANDBOX_PROXY_HOST="" disables the proxy
      # posture (sandbox containers fall back to the legacy 4-key-env /
      # cap_drop=ALL / user=1000:1000 posture); leaving the default routes
      # through the in-cluster proxy.
      - SANDBOX_PROXY_HOST=${SANDBOX_PROXY_HOST-sandbox-proxy}
      - SANDBOX_PROXY_PORT=${SANDBOX_PROXY_PORT:-8080}
      # CA volume name is hardcoded in configs.SANDBOX_PROXY_CA_VOLUME_NAME;
      # source of truth is the `volumes:` block below + install.sh pre-create.
    volumes:
      - ${SANDBOX_DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
    networks:
      # Bridge join lets api_server proxy the Next.js preview iframe.
      - default
      - onyx_craft_sandbox
    # service_healthy (stricter than the base file's service_started pattern) so
    # a sandbox provisioned right after stack start can actually reach the
    # proxy; service_started fires too early -- the proxy binds its listener
    # only after CA bootstrap + initial sync (see _build_lookup in server.py).
    # Merges with the base depends_on map.
    depends_on:
      sandbox-proxy:
        condition: service_healthy

  background:
    environment:
      # Must match api_server so the idle-cleanup celery task uses the same
      # backend to snapshot + terminate sandboxes.
      - SANDBOX_BACKEND=${SANDBOX_BACKEND:-docker}
      - SANDBOX_API_SERVER_URL=${SANDBOX_API_SERVER_URL:-}
      - SANDBOX_CONTAINER_IMAGE=${SANDBOX_CONTAINER_IMAGE:-onyxdotapp/sandbox:v0.1.50}
      - SANDBOX_DOCKER_NETWORK=${SANDBOX_DOCKER_NETWORK:-onyx_craft_sandbox}
      - SANDBOX_DOCKER_MEMORY_LIMIT=${SANDBOX_DOCKER_MEMORY_LIMIT:-2g}
      - SANDBOX_DOCKER_CPU_LIMIT=${SANDBOX_DOCKER_CPU_LIMIT:-1.0}
      - ENABLE_OPENCODE_DEBUGGING=${ENABLE_OPENCODE_DEBUGGING:-false}
      - SANDBOX_PROXY_HOST=${SANDBOX_PROXY_HOST-sandbox-proxy}
      - SANDBOX_PROXY_PORT=${SANDBOX_PROXY_PORT:-8080}
      # CA volume name is hardcoded in configs.SANDBOX_PROXY_CA_VOLUME_NAME;
      # source of truth is the `volumes:` block below + install.sh pre-create.
    volumes:
      - ${SANDBOX_DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
    networks:
      - default
      - onyx_craft_sandbox
    # See api_server above for the service_healthy rationale.
    depends_on:
      sandbox-proxy:
        condition: service_healthy

  # Egress proxy that MITMs sandbox HTTPS traffic and gates action approvals.
  # Reuses the onyx-backend image; just a different entrypoint. The proxy speaks
  # to Postgres and Redis directly (same code path as api_server) and to the
  # Docker socket (RO, events watcher only). Cross-process state with api_server
  # lives in `action_approval` rows + Redis announce/wake lists, never in HTTP
  # between the two.
  sandbox-proxy:
    image: ${ONYX_BACKEND_IMAGE:-onyxdotapp/onyx-backend:${IMAGE_TAG:-latest}}
    command: ["python", "-m", "onyx.sandbox_proxy.server"]
    env_file:
      - path: .env
        required: false
    environment:
      - SANDBOX_BACKEND=docker
      - POSTGRES_HOST=${POSTGRES_HOST:-relational_db}
      - REDIS_HOST=${REDIS_HOST:-cache}
      # Do NOT export SANDBOX_DOCKER_SOCKET into the container. The volume mount
      # below maps ${SANDBOX_DOCKER_SOCKET} (host path) to /var/run/docker.sock
      # (container path); exporting the host path would make Python try to
      # connect to a socket that doesn't exist inside the container (breaks
      # Colima / non-default Linux setups). Python's default is
      # /var/run/docker.sock, which is exactly where the bind mount lands.
      - SANDBOX_DOCKER_NETWORK=${SANDBOX_DOCKER_NETWORK:-onyx_craft_sandbox}
      - SANDBOX_PROXY_LISTEN_PORT=${SANDBOX_PROXY_PORT:-8080}
      # Healthz port intentionally NOT wired through env. Compose's healthcheck
      # test below hardcodes 8081 and can't read the container's env, so
      # exposing SANDBOX_PROXY_HEALTHZ_PORT as a knob would let it silently
      # desync from the probe. Python's default
      # (configs.SANDBOX_PROXY_HEALTHZ_PORT = 8081) is the source of truth for
      # compose deployments.
      # FileCAStore writes the CA to configs.SANDBOX_PROXY_CA_VOLUME_PATH
      # (hardcoded). The `volumes:` block below bind-mounts the same name at
      # that path; sandbox containers mount it read-only at /sandbox-ca.
    volumes:
      - sandbox_proxy_ca:/var/lib/sandbox-proxy/ca
      # RO: the proxy only inspects (events watcher + initial sync).
      - ${SANDBOX_DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock:ro
    depends_on:
      relational_db:
        condition: service_started
      cache:
        condition: service_started
    networks:
      # `default` reaches relational_db + cache by compose-DNS; the sandbox
      # bridge join makes the service resolvable as `sandbox-proxy` from inside
      # each sandbox container.
      - default
      - onyx_craft_sandbox
    restart: unless-stopped
    # >= _DRAIN_TIMEOUT_S (10s in server.py) plus margin so the SIGTERM drain
    # can terminalize parked approvals (claim EXPIRED + wake BLPOP listeners,
    # see gate._terminalize_after_unhandled_error) before docker tears the
    # container down. Matches K8s terminationGracePeriodSeconds.
    stop_grace_period: 20s
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8081/healthz')",
        ]
      interval: 10s
      timeout: 3s
      retries: 3
      # Matches server._LOOKUP_INITIAL_SYNC_TIMEOUT_S so a slow initial sync
      # doesn't race the healthcheck: if sync wins, next probe flips healthy; if
      # it loses, the process exits and docker restarts.
      start_period: 60s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "6"

networks:
  # Pre-created by install.sh --include-craft.
  onyx_craft_sandbox:
    name: onyx_craft_sandbox
    external: true

volumes:
  # Pre-created by install.sh --include-craft. external+name avoids the compose
  # project prefix so DockerSandboxManager can mount the same name when it
  # provisions sandbox containers from outside compose's project scope.
  sandbox_proxy_ca:
    name: sandbox_proxy_ca
    external: true
