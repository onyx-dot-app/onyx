# =============================================================================
# ACTIVA LITE — MINIMAL DEPLOYMENT VALUES
# =============================================================================
# Minimal Activa deployment: no vector database, no Redis, no model servers.
# Only PostgreSQL is required. Connectors and RAG search are disabled, but the
# core chat experience (LLM conversations, tools, user file uploads, Projects,
# Agent knowledge) still works.
#
# Usage:
#   helm install activa ./deployment/helm/charts/activa \
#     -f ./deployment/helm/charts/activa/values-lite.yaml
#
# Or merged with your own overrides:
#   helm install activa ./deployment/helm/charts/activa \
#     -f ./deployment/helm/charts/activa/values-lite.yaml \
#     -f my-overrides.yaml
# =============================================================================

vectorDB:
  enabled: false

vespa:
  enabled: false

redis:
  enabled: false

configMap:
  CACHE_BACKEND: "postgres"
  AUTH_BACKEND: "postgres"
  FILE_STORE_BACKEND: "postgres"
