// Shared mobile constants.

/**
 * The `in_code_tool_id` of the built-in internal-search tool. Used to identify
 * the Search tool in the agent's tool list (e.g. for the source<->search
 * coupling in the actions popover). Mirrors web's SEARCH_TOOL_ID.
 */
export const SEARCH_TOOL_ID = "SearchTool";
