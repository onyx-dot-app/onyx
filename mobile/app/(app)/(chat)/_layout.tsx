// Chat tab is a single screen (index) — no [sessionId] route; it loads the current session in place.
import { HiddenHeaderStack } from "@/components/HiddenHeaderStack";

export default HiddenHeaderStack;
