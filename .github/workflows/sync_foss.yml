name: Sync FOSS Repo

on:
  schedule:
    # Run daily at 3am PT (11am UTC during PST)
    - cron: "0 11 * * *"
  workflow_dispatch:

jobs:
  sync-foss:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - name: Checkout main repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install git-filter-repo
        run: |
          sudo apt-get update && sudo apt-get install -y git-filter-repo

      - name: Configure SSH for deploy key
        env:
          FOSS_REPO_DEPLOY_KEY: ${{ secrets.FOSS_REPO_DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$FOSS_REPO_DEPLOY_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Set Git config
        run: |
          git config --global user.name "activa-bot"
          git config --global user.email "bot@activa.ai"

      - name: Build FOSS version
        run: bash backend/scripts/make_foss_repo.sh

      - name: Push to FOSS repo
        env:
          FOSS_REPO_URL: ${{ vars.FOSS_REPO_URL }}
        run: |
          if [ -z "$FOSS_REPO_URL" ]; then
            echo "[ERROR] Define the FOSS repository in vars.FOSS_REPO_URL"
            exit 1
          fi
          cd /tmp/foss_repo
          git remote add public "$FOSS_REPO_URL"
          git push --force public main
