### Syncs the vendored .cursor/skills/greptile directory (plus its top-level
### skill symlinks) from github.com/greptileai/skills via
### .cursor/skills/sync-greptile-skills.sh, and opens a reviewed PR when
### upstream changed. The PR title carries the upstream commit, so provenance
### survives the squash merge.

name: Update Greptile Skills

on:
  schedule:
    - cron: "0 14 * * 1" # weekly, Monday 14:00 UTC (off the 13:00 recommended-models slot)
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: update-greptile-skills
  cancel-in-progress: false

jobs:
  update-greptile-skills:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Mint GitHub App installation token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          client-id: ${{ vars.CHERRY_PICK_APP_ID }}
          private-key: ${{ secrets.CHERRY_PICK_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # ratchet:actions/checkout@v6
        with:
          persist-credentials: true
          ref: main
          token: ${{ steps.app-token.outputs.token }}

      - name: Configure git identity as App
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
        run: |
          bot_user_id="$(gh api "/users/${APP_SLUG}[bot]" --jq .id)"
          git config user.name "${APP_SLUG}[bot]"
          git config user.email "${bot_user_id}+${APP_SLUG}[bot]@users.noreply.github.com"

      - name: Sync greptile skills from upstream
        id: sync
        run: |
          before="$(git rev-parse HEAD)"
          ./.cursor/skills/sync-greptile-skills.sh
          if [ "$(git rev-parse HEAD)" != "${before}" ]; then
            echo "changed=true" >> "${GITHUB_OUTPUT}"
          fi

      - name: Open or update PR
        if: steps.sync.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        # Fixed branch + force-push: an already-open sync PR should be
        # refreshed with the newest upstream state, not left stale. The sync
        # commit subject (chore(skills): sync greptile skills to
        # greptileai/skills@<sha>) becomes the PR title.
        run: |
          branch="auto/update-greptile-skills"
          title="$(git log -1 --format=%s)"
          git switch -C "${branch}"
          git push --force origin "${branch}"

          existing_pr="$(gh pr list --state open --head "${branch}" --json number --jq '.[0].number')"
          if [ -n "${existing_pr}" ]; then
            gh pr edit "${existing_pr}" --title "${title}"
            echo "Refreshed existing PR #${existing_pr} via force-push."
          else
            gh pr create \
              --base main \
              --head "${branch}" \
              --title "${title}" \
              --body "Generated by the [Update Greptile Skills workflow run](${RUN_URL}). Vendored from [greptileai/skills](https://github.com/greptileai/skills) by \`.cursor/skills/sync-greptile-skills.sh\`."
          fi

  notify-slack-on-failure:
    needs:
      - update-greptile-skills
    if: always() && needs.update-greptile-skills.result == 'failure'
    runs-on: ubuntu-latest
    environment: ci-protected
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # ratchet:actions/checkout@v6
        with:
          persist-credentials: false
          sparse-checkout: .github/actions/slack-notify

      - name: Notify Slack about update failure
        uses: ./.github/actions/slack-notify
        with:
          webhook-url: ${{ secrets.MONITOR_DEPLOYMENTS_WEBHOOK }}
          title: "🚨 Update Greptile Skills workflow failed"
          details: "*The weekly greptile skills sync failed.* Check the run logs."
